name = "agent0-semantic-search"
main = "worker/src/index.ts"
compatibility_date = "2024-11-25"

# Environment variables (set via wrangler secret put or .dev.vars)
# VENICE_API_KEY - Required: Venice AI API key for embeddings
# PINECONE_API_KEY - Required: Pinecone API key
# PINECONE_INDEX - Required: Pinecone index name
# PINECONE_NAMESPACE - Optional: Pinecone namespace

[vars]
# Add any non-secret environment variables here

# Cron triggers (commented out for now - will be enabled in future commits)
# [[triggers.crons]]
# cron = "*/15 * * * *"  # Every 15 minutes

# D1 database (commented out for now - will be enabled for sync state storage)
# [[d1_databases]]
# binding = "DB"
# database_name = "semantic-sync-state"
# database_id = "your-database-id-here"

